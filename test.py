# avg.py
# calculates the average of two exam scores
def main():
    print ("This program computes the average of two scores")

    score1, score2 = eval(input("Enter the two scores separated by a comma:"))
    average = (score1 + score2) /2

    print("The average of the score is: ", average)
                                
